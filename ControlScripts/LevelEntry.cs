using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelEntry : MonoBehaviour
{

    /*public Sprite spriteCompleted;
    public Sprite spriteNotCompleted;
    public Sprite spriteLocked;


    public void UpdateSprite(LevelStatus status)
    {
        SpriteRenderer spriteRenderer = GetComponent<SpriteRenderer>();

        switch (status)
        {
            case LevelStatus.Completed:
                spriteRenderer.sprite = spriteCompleted;
                break;

            case LevelStatus.NotCompleted:
                spriteRenderer.sprite = spriteNotCompleted;
                break;

            case LevelStatus.Locked:
                spriteRenderer.sprite = spriteLocked;
                break;
        }
    }*/
}
